# Shinobu Translator

Shinobu Translator 扩展与 Web 工作台共用的领域词汇，明确漫画图片翻译流程中容易混淆的产品概念。

## Language

**大模型译文质量**:
大模型根据 OCR 文本生成译文时的语义准确性、上下文连贯性与表达自然度；不包含文本检测、OCR、去字或排版的质量。
_Avoid_: 翻译质量、最终译图质量

**快速翻译模式**:
面向所有文本大模型供应商、在继续使用用户当前所选模型的前提下，将相同整页 OCR 输入的大模型文本翻译阶段中位完成时间至少缩短一半的执行模式；它可以通过增加同页请求数与 token 用量来缩短等待时间，仅提前展示部分译文不算完成时间缩短。Google 网页翻译和 Nano Banana 整图翻译都不属于此模式。
_Avoid_: 低质量模式、快速模型模式

**整页翻译上下文**:
按阅读顺序组织的一页全部 OCR 文本框信息；快速翻译模式中的每个并行分片都能看到它，即使该分片只负责返回其中一部分译文。
_Avoid_: 分片上下文、局部上下文

**翻译分片**:
按阅读顺序连续、以 OCR 有效字符量均衡的一组完整文本框；一次大模型请求拥有整页翻译上下文，但只返回其翻译分片内的译文。
_Avoid_: 页面分片、局部翻译

**大模型排版分段**:
大模型在生成完整译文之外，为视觉排版返回的列或行分段；它影响嵌字布局，但不属于大模型译文质量。
_Avoid_: 译文、语义翻译

**翻译恢复请求**:
翻译分片部分失败后，携带整页翻译上下文、仅补齐失败或缺失文本框的一次合并请求；已成功的分片译文不会被丢弃或重译。
_Avoid_: 整页重试、逐框重试

**流水线自动重试**:
本地图片流水线执行遇到可恢复的临时故障后，在同一次 execution 与同一个图片任务身份内只重新进行最近失败且可安全重复的操作；用户手动重试会开始新的 execution，不属于流水线自动重试。
_Avoid_: 翻译恢复请求、图片任务重试、新图片任务

**图片翻译执行**:
为一张输入图片生成译图的一次执行尝试；它可以采用本地图片流水线执行或 Nano Banana 整图翻译，二者共享任务生命周期但不共享阶段、配置和结果结构。
_Avoid_: 图片任务、处理批次、统一图片流水线

**图片翻译执行活动**:
由同一个图片翻译执行拥有者在一项产品意图下管理的连续生命周期；它可以包含零次、一次或多次图片翻译执行，而活动内的执行顺序仍由该拥有者决定。
_Avoid_: 图片任务、处理批次、单次执行

**本地图片流水线执行**:
对一张工作副本依次完成文本检测、OCR、文本翻译、去字与排版，并生成译图及结构化处理产物的过程；“本地”表示图片处理留在浏览器内，文本翻译仍可使用用户选择的外部服务，不包括 Nano Banana 整图翻译。
_Avoid_: 共享流水线、完全离线翻译、Nano Banana 流水线

**流水线处理记录**:
一次成功的本地图片流水线执行所产生的结构化记录，用于关联 OCR 文本框、对应译文和必要的版面信息；它与译图共同构成统一返回的规范执行结果，各宿主自行决定是否持久化，但它不是扩展与 Web 之间的交换格式。
其中所有几何均使用工作副本像素坐标，并以显式 transform 描述原图与工作副本的关系。
_Avoid_: WebPipelineRecord、诊断摘要、项目包

**图片翻译执行拥有者**:
管理一项图片翻译执行活动并决定其中何时发起图片翻译执行的产品角色，活动内的执行顺序与优先级由它负责；活动结束或被替代时必须请求取消，之后产生的迟到进度与结果不得再交付。
_Avoid_: Worker、Port、UI 组件

**图片翻译执行仲裁器**:
在同一个 content context 内保证当前的图片翻译执行活动只属于一个图片翻译执行拥有者；来自不同拥有者的新显式用户活动会替代当前拥有者的全部活动，自动活动不能替代其他拥有者的显式活动，替代时先停止旧活动交付再请求取消。它不排列同一拥有者的活动或活动内的图片任务、不执行本地图片流水线或 Nano Banana 整图翻译，也不定义跨标签页优先级。
_Avoid_: Executor、全局资源队列、处理批次

**本地流水线准入协调器**:
扩展 background 中协调所有标签页共享唯一 offscreen runtime 的资源边界；它按全局到达顺序放行本地图片流水线执行，但不理解截图、阅读模式或连续翻译等产品优先级。
_Avoid_: 图片翻译执行仲裁器、offscreen executor、跨标签页产品调度

**流水线执行进度**:
本地图片流水线执行当前阶段、操作、完成数量和重试状态的结构化信息；宿主据此生成显示文案，但不得从文案反推执行结果或故障分类。
_Avoid_: 进度文案、状态提示、错误消息

**无可翻译文本**:
本地图片流水线执行成功确认输入中没有可翻译文本的非错误结果；它返回输入等价的结果图与空的流水线处理记录，不触发重试或暂停。
_Avoid_: OCR 失败、图片任务失败、流水线执行取消

**图片局部故障**:
仅表明当前输入图片无法完成本地图片流水线执行的故障；它使当前图片任务失败，但不阻止拥有者继续处理其他图片。
_Avoid_: 流水线运行环境故障、批次故障

**流水线运行环境故障**:
表明模型、配置、凭据或外部文本翻译服务等运行条件当前无法继续可靠执行的故障；拥有者必须暂停领取新任务，直到运行条件恢复。
_Avoid_: 图片局部故障、单图失败

**连续翻译模式**:
用户主动在当前标签页开启、持续将视口附近符合条件的漫画图片送入自动翻译流程的页面会话；同站刷新延续会话，跨域后结束。
_Avoid_: 自动翻译模式、翻译全部、批量翻译模式

**连续翻译显示模式**:
连续翻译页面统一选择显示原图或已完成译图的状态；它独立于任务正在运行、暂停或结束。
_Avoid_: 翻译状态、任务模式

### 浏览器扩展

**扩展翻译默认配置**:
用户为浏览器扩展后续图片翻译执行选择的可编辑默认值，包括目标语言、处理模式、供应商目标、模型与翻译提示等；修改只影响尚未开始的图片翻译执行。它不包含供应商凭据、授权会话、界面显示偏好或诊断开关。
_Avoid_: ExtensionSettings、当前执行配置、供应商访问状态

**扩展执行配置快照**:
浏览器扩展在一次图片翻译执行开始时，从当时已提交的扩展翻译默认配置生成并完成规范化的不可变运行参数；该执行后续始终使用同一快照，默认配置的并发修改只影响之后开始的执行。快照携带非敏感执行语义与供应商目标，不携带 API Key、OAuth 令牌或网页会话信息。
_Avoid_: 扩展翻译默认配置、实时设置、锁定处理配置

**供应商访问状态**:
浏览器扩展 background 根据 API Key、OAuth 授权、网页会话与浏览器权限等实际条件得出的供应商目标可用性，以及用户恢复可用性所需的下一步操作；background 是该状态的权威来源，Popup 中的缓存或提示不能改变它。状态投影不得包含实际凭据或令牌；实际 API Key 只能由经过扩展文档身份校验的 Popup 通过显式、单次的凭据揭示能力读取，不得进入通用设置投影、变更订阅、Content 执行快照或诊断日志。
_Avoid_: 登录按钮状态、Popup 缓存、供应商凭据

**扩展界面偏好**:
只影响浏览器扩展界面呈现或调试体验、不会改变图片翻译执行语义的用户选择；它与扩展翻译默认配置分别持久化和演进，由扩展控制提交头原子切换同一代记录。
_Avoid_: 扩展翻译默认配置、流水线配置

### Web 工作台

**Web 工作台实例**:
用户在一个浏览器标签页中打开的独立工作区；它同时最多承载一个处理批次草稿、处理批次恢复准备或活动处理批次。多个实例可以并行处理不同的处理批次，但同一处理批次身份同时最多只能由一个实例恢复或运行；其他实例仍可查看和导出该批次，但不能恢复、克隆、删除或仅保留结果，直到占用解除。
_Avoid_: 浏览器会话、处理批次、全局工作台

**锁定处理配置**:
用户启动处理批次时从处理批次草稿固化的处理选择；它只包含重现本批次处理所需的目标语言、处理模式、所选供应商目标与模型等知识，在原处理批次内不可修改，并随本地历史和项目包保存。它不包含界面语言、未参与本批次的供应商配置或用户自备凭据。
_Avoid_: WebSettings、Web 偏好、批次设置

**锁定处理配置不可用**:
处理批次保存的锁定处理配置结构完整，但当前版本已不能执行其中的供应商目标；该处理批次仍可查看和导出，但不能恢复。克隆时必须先为新的处理批次草稿选择当前可用的供应商目标。
_Avoid_: 配置损坏、凭据缺失

**处理批次**:
由用户明确启动、共享一份锁定处理配置并按顺序处理的一组图片任务；同一个 Web 工作台同时最多有一个活动处理批次。运行期间可以追加图片任务，连续相机从打开到退出属于同一个处理批次，每次成功拍摄追加一个图片任务。改变配置或重跑历史时会创建新的处理批次。
_Avoid_: 会话、队列、项目

**处理批次草稿**:
用户尚未启动的待处理图片与可编辑处理配置；它可以来自当前导入或克隆本地历史，不拥有处理批次身份，也不进入本地历史，直到用户明确开始处理。
_Avoid_: 未开始的处理批次、临时批次

**处理批次恢复准备**:
从本地历史继续原处理批次前，原图、图片任务身份、恢复点与锁定处理配置已经完整校验并装入工作台，但尚未重新开始领取图片任务的状态；它保留原处理批次身份且配置不可编辑，可以等待用户重新提供匹配目标的用户自备凭据。退出该状态会丢弃工作台中的恢复准备，不改变原本地历史，也不会隐式转成处理批次草稿。
_Avoid_: 处理批次草稿、活动处理批次、克隆

**图片任务**:
处理批次中对应一张输入图片的独立工作单元；它可以完成、失败、取消或等待恢复，而不改变同批次其他图片任务的身份。在原处理批次内按锁定配置重试时，它仍是同一个图片任务。
_Avoid_: 页面、文件、批次

**部分完成**:
处理批次不再自动领取图片任务，且其中既有已完成任务，也有失败或取消任务的终态；它不表示批次仍在等待恢复。
_Avoid_: 处理批次失败、暂停

**连续相机**:
Web 工作台中用户主动打开、在关闭前将每次成功拍摄追加为同一处理批次图片任务的采集方式；关闭只结束采集，不取消正在执行的图片任务。未产生图片任务便退出时，该空批次不进入本地历史。
_Avoid_: 单图相机、每拍一批次

**工作副本**:
供本地图片流水线实际处理的逻辑图片输入；它可以按原图直用策略沿用原图，也可以经过旋转、铺白或下采样形成规范化输入，但绝不会覆盖原图。
_Avoid_: 原图、缩略图、结果图

**本地历史**:
浏览器为继续处理和回看结果而保存的处理批次集合；它是可能被浏览器清理的便利缓存，不是永久备份。活动处理批次只锁定与其身份或资源冲突的历史操作，不冻结其他批次的查看、导入、导出或清理。
_Avoid_: 云端历史、备份、账户数据

**本地历史写入故障**:
图片任务结果已在当前页面生成、但未能写入本地历史的状态；它不属于图片任务失败，发生后处理批次暂停领取新的图片任务。
_Avoid_: 图片任务失败、翻译失败

**本地资源清理故障**:
本地历史中的删除或仅保留结果已经完成逻辑提交，但一个或多个不再引用的本地资源尚未成功清除；它不会恢复原处理批次或其可恢复性，浏览器会继续重试清理并报告尚未释放的空间。
_Avoid_: 本地历史写入故障、删除失败

**本地历史部分损坏**:
本地历史中的处理批次索引仍然存在，但其引用的一个或多个本地资源已经缺失或无效；尚存结果仍可查看、下载或导出，但该处理批次不能恢复、克隆或导出为项目包。
_Avoid_: 部分完成、处理批次失败

**仅保留结果**:
删除本地历史中某个处理批次的原图和缩略图、只留下已生成结果的空间清理操作；提交后该处理批次不能恢复或克隆。它先进入十秒可撤销等待期，到期后才真正清理本地资源；等待期内刷新或关闭页面视为撤销。
_Avoid_: 删除处理批次、导出结果 ZIP

**待删除**:
用户已请求删除本地历史中的处理批次、但可撤销等待期尚未结束的状态；等待期内可以撤销，刷新或关闭发起操作的 Web 工作台实例也视为撤销，到期后才真正删除该处理批次及其本地资源。等待期间其他实例仍可查看和导出该批次，但不能恢复、克隆、删除或仅保留结果。
_Avoid_: 已删除、回收站

**项目包**:
用户主动导出的、可在另一个受支持浏览器中重新导入的完整处理批次归档；它不包含 API Key 或模型资产。导入项目包会创建新的本地处理批次身份，不覆盖现有历史，也不替换当前处理批次草稿或活动处理批次。
_Avoid_: 结果 ZIP、缓存备份、模型包

**结果 ZIP**:
只为交付译图而导出的一个或多个结果文件集合；它不能恢复处理批次。来源为本地历史部分损坏时可以只包含尚存结果，但必须明确披露未导出的图片任务及缺失原因。
_Avoid_: 项目包、历史备份

**用户自备凭据**:
用户为文本翻译服务提供并自行承担配额与费用的 API Key；Shinobu Translator 不提供共享翻译额度。处理批次启动时锁定本次使用的凭据；凭据不进入本地历史或项目包，恢复批次时需重新取得与原提供商目标匹配的凭据。
_Avoid_: 平台 Key、免费额度、账户凭据

**设备能力档位**:
设备通过实际运行测试后获得的本地处理能力等级；它决定该设备可启动的图片尺寸，而不是对硬件型号的推测。
_Avoid_: 浏览器版本、设备评分、性能模式
