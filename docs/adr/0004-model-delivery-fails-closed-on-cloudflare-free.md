---
status: accepted
---

# 模型分发在 Cloudflare 免费额度内 fail-closed

模型保存在私有 R2，由 Workers Free 网关只公开应用内置清单中的内容哈希文件，并在免费 Worker 日配额耗尽时停止服务。相比公共 R2 域名或无限量下载，这一方案主动选择“遭滥用时暂时不可用”而不是“继续放量并可能计费”；它只能约束本项目的公开下载路径，不能保证同一 Cloudflare 账户中的其他使用永不产生费用。
